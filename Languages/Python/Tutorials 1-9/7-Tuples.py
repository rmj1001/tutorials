# A tuple is a list, 
# but a tuple is inmutable (can't be changed or modified), 
# unlike a list

# Initializes a tuple [var = (e1, e2, etc...)]
coordinates = (4, 5)

# Prints a tuple at the specified position 
# (0 being the first position)
print(coordinates[0])